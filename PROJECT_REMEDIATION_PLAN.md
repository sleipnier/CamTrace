# 项目结构与持久化整改计划

> 本文基于 2026-07-25 的只读检查结果编写。当前阶段只定位问题和规划整改，不修改业务代码、不删除文件、不迁移数据。

## 1. 结论摘要

项目当前的两个现象并不是同一个问题：

1. **代码目录杂乱**：根目录混合了源码、前端构建产物、运行日志/PID/状态文件、媒体和数据集、模型与缓存、临时输出、历史备份，以及一个嵌套的独立 Git 仓库。更严重的是，启动脚本默认指向嵌套副本，导致“编辑的代码”和“实际运行的代码”可能不是同一份。
2. **没有数据库文件**：当前实现没有数据库依赖，也没有 SQL/ORM 初始化逻辑。任务和可视化状态被设计为 `VTC_RUNTIME_ROOT/api` 下的 JSON 文件（任务为 `jobs/<id>.json`，可视化为 `visualizations/<id>/state.json`），上传、结果和错误日志分别放在运行时目录中。因此数据库不是漏提交，而是当前架构的明确选择；不过它只适合单机、低并发和临时部署，存在并发一致性、查询能力、备份和多实例扩展限制。

## 2. 已确认的证据

### 2.1 目录和版本库问题

- 根目录同时包含 `api.py`、`service.py`、前端 `src/`、`tests/`，以及大量 `*.log`、`*.status`、`*.pid`、`*.mp4`、`*.zip`、诊断报告和模型预览文件。
- 根目录还包含 `mega-sam/`（外部模型/实验仓库）、`.pylibs/`、`.torch/`、`huggingface/`、`cache/` 等运行环境和模型缓存；工作区总量约 15 GB，其中若干缓存和模型目录达到 GB 级。
- 根目录下存在 `videotocamera/.git/`，它是同一远程仓库的另一个独立 clone，版本停留在较旧的 `main`；根仓库当前为 `master`，并包含较新的 Web 集成提交。两份副本中的同名 Python/README 文件已确认不一致。
- `hyperai_start.sh` 的默认值是 `VTC_PROJECT_ROOT=/hyperai/home/videotocamera`，因此未显式设置环境变量时会优先运行嵌套副本；当前运行中的进程则显示为 `/hyperai/home/api_server.py`，说明环境中已经出现了多条启动路径。
- Git 跟踪清单包含 `frontend/dist` 构建产物；而工作区又保留未跟踪的嵌套仓库、模型仓库和大量本地运行结果，说明“源代码仓库”和“部署工作目录”没有边界。
- `.gitignore` 已忽略不少运行生成物，但忽略规则是在产物已经进入工作区之后补充的，不能解决已跟踪文件、嵌套仓库和历史大对象问题；根仓库 `.git` 本身约 4 GB，也需要单独做历史体积治理。

### 2.2 持久化与数据库问题

- `requirements.txt` 只包含 FastAPI、Uvicorn、multipart、科学计算和可视化依赖，没有 SQLite/SQLAlchemy/Alembic/数据库驱动。
- `api.py` 将 `JOB_ROOT` 定义为 `runtime_root / "api" / "jobs"`，通过 `write_json_atomic()` 写入 JSON；可视化状态同样写入 `visualizations/<id>/state.json`。
- 服务启动时通过扫描 JSON 文件恢复任务，并把未完成任务标记为 `SERVICE_RESTARTED`；结果压缩包按 TTL 和空间上限清理。这说明当前持久化模型是“文件状态 + 文件制品”，不是数据库模型。
- `vtc-runtime/api/jobs/` 当前确实存在任务 JSON，`vtc-runtime/results/` 当前确实存在结果 ZIP；所谓“没有数据库文件”在现架构下是预期现象。
- 个别任务记录中的 `archivePath` 出现 `/openbayes/home/...` 与 `/hyperai/home/...` 两种路径前缀，表明同一持久化数据还存在环境路径别名/挂载路径不统一风险。

## 3. 根因判断与风险

### P0：运行入口不唯一

根仓库、嵌套 clone、当前进程和启动脚本对项目根目录的认知不一致。风险是修复提交后服务仍运行旧代码，或结果由不同版本产生，排障和回滚都不可靠。

### P1：源码、部署、实验和缓存共用一个工作区

没有明确的 `src/`、`scripts/`、`docs/`、`tests/`、`deploy/`、`runtime/`、`artifacts/` 边界；外部仓库和模型缓存也放在项目根下。风险是误提交、误覆盖、备份膨胀和新成员无法判断哪些文件可编辑。

### P1：生成物治理不完整

日志、状态文件、视频、压缩包、前端 `dist`、Python 缓存和诊断报告散落在根目录；部分产物虽被忽略，仍会污染本地操作和部署包，且历史 Git 对象可能继续占用空间。

### P1：JSON 文件持久化能力有限

单文件原子替换可以避免部分写入损坏，但不能提供索引、事务、条件更新、跨实例锁和审计查询。随着任务量或并发实例增加，任务列表扫描、状态竞争、备份恢复和清理策略都会变复杂。

### P2：路径配置没有单一规范

环境变量、默认路径和已落盘的绝对路径不统一；容器重启、挂载点变化或切换启动方式后，任务记录可能指向不可访问的结果文件。

## 4. 整改目标

- 只保留一个受版本控制的项目根和一个明确的启动入口。
- 将外部 MegaSaM、模型、Python 包、缓存、运行时数据与源码彻底分离。
- 让 Git 只跟踪源代码、配置模板、文档、测试和必要的可复现构建描述。
- 明确定义“任务元数据”“结果制品”“日志”的生命周期和备份策略。
- 在不改变 API 契约的前提下，为数据库迁移保留稳定的 Repository/Storage 抽象。
- 统一所有持久化路径为运行时相对路径或可配置根路径，禁止把机器绝对路径写入跨环境数据。

## 5. 分阶段实施计划

### 阶段 0：冻结与盘点（先做）

- 记录当前服务进程、启动命令、Git commit、环境变量和 `VTC_RUNTIME_ROOT`。
- 备份 `vtc-runtime/api`、`results`、`errors` 及仍需保留的用户数据，并计算校验和。
- 列出根目录每个文件/目录的分类、所有者、是否应入 Git、保留期限和迁移目标。
- 明确唯一规范路径（建议项目固定为 `/hyperai/home/videotocamera` 或当前实际根路径二选一），所有脚本和文档只引用该路径。

### 阶段 1：消除双仓库和入口歧义

- 选择根仓库或 `videotocamera/` 作为唯一主线；对比两边提交后只保留一份源码。
- 将启动脚本改为显式校验项目根、commit 和关键文件，未设置路径时失败而不是静默猜测旧副本。
- 统一 `api_server.py`、前端构建和测试的执行入口；增加启动时输出版本号/commit 的健康信息。
- 在确认无进程使用后，再将嵌套 clone、`videotocamera.pre-git-20260724/` 等备份移出项目根或归档。

### 阶段 2：重划目录与生成物边界

建议目标结构（名称可按部署约束调整）：

```text
project/
  src/                  # Python/React 源码（或保留现有根级模块，二选一）
  tests/
  docs/
  scripts/
  deploy/
  frontend/
  requirements.txt
  .env.example
runtime/                # 不入 Git：jobs、uploads、results、errors、logs
external/               # 不入 Git：mega-sam、模型和补丁
cache/                  # 不入 Git：HF、Torch、Python/Node 缓存
artifacts/              # 不入 Git：视频、ZIP、渲染结果
```

- 先迁移并验证，再删除或归档根目录中的日志、媒体、状态、缓存和诊断文件；不得直接递归删除未经盘点的目录。
- `frontend/dist` 明确选择“构建时生成”或“发布包内置”一种策略；若选择前者则取消 Git 跟踪并在 CI/部署脚本构建。
- 完善 `.gitignore`，并检查已跟踪生成物；必要时使用 Git LFS 或发布制品存储，而不是把大文件继续放入 Git 历史。
- 对根仓库历史大对象做专门审计和压缩/迁移方案，先在镜像仓库演练，避免破坏协作者已有 clone。

### 阶段 3：持久化方案决策（建议先 SQLite，保留升级路径）

**短期建议：SQLite + 文件制品目录。** 任务状态、来源元数据、错误、时间戳、结果路径和可视化状态进入一个运行时 SQLite 文件；视频、ZIP、日志仍存文件系统，数据库只保存相对路径、大小和 SHA-256。这样可以获得索引、事务、分页和一致更新，部署复杂度仍接近单机方案。

- 引入 schema 版本和迁移工具（例如 Alembic 或等价机制），首次启动执行显式迁移，不自动覆盖旧数据。
- 建立 JSON-to-SQLite 一次性导入工具：逐条校验、保留原 JSON 备份、记录失败项并支持幂等重跑。
- 统一 Repository 接口，使 API 层不直接读写 JSON；迁移期间可提供只读回退，但不能双写无一致性策略。
- 结果路径改为相对 `VTC_RUNTIME_ROOT` 的路径；启动时校验文件存在性，避免 `/openbayes/home` 与 `/hyperai/home` 别名污染数据。

**中长期条件：** 如果需要多副本服务、跨机器共享、复杂检索或更强审计，再迁移 PostgreSQL；对象/大文件则使用对象存储或专用制品存储。不要为了“必须有数据库文件”直接引入远程数据库而忽略部署和备份成本。

### 阶段 4：验证、切换和回滚

- 为任务创建、状态转移、服务重启恢复、取消、TTL 清理、结果缺失和分页增加集成测试。
- 在副本环境导入现有 JSON，逐条比对 API 返回、结果下载和可视化状态。
- 采用“停止写入 → 备份 → 导入 → 校验 → 切换 → 观察”的窗口切换；保留原 JSON 和旧启动方式作为短期回滚入口。
- 上线后一周监控数据库大小、锁等待、失败率、结果过期率和磁盘占用，再决定是否需要 PostgreSQL/对象存储。

## 6. 验收标准

- `git status` 不再出现运行日志、媒体、缓存、嵌套仓库和临时状态；新成员可从 README 的单一命令启动正确 commit。
- 服务进程、健康检查和前端静态资源来自同一个项目根和同一个版本标识。
- 任务列表不依赖遍历大量 JSON 文件；状态更新具备事务语义，重启和并发测试通过。
- 数据库和制品可独立备份/恢复；恢复后任务元数据、结果下载和过期清理行为与迁移前一致。
- 文档明确源码、外部依赖、缓存、运行数据和发布制品的存放位置及生命周期。

## 7. 本轮已落地内容

- 新增 `state_store.py`：使用 SQLite 保存任务和可视化状态，启用 WAL、索引和事务式 upsert。
- `api.py` 已切换到 SQLite，并保留旧 JSON 的启动时惰性导入；原 JSON 不会被自动删除。
- 新增 `migrate_json_to_sqlite.py`，支持幂等迁移和显式运行。
- `hyperai_start.sh` 默认使用唯一主仓库根目录，并在入口处校验 `api_server.py`。
- `.gitignore` 已补充 SQLite/WAL 文件规则；README 已补充持久化、迁移和入口说明。
- 新增 SQLite 存储单元测试；现有 41 个测试及新增测试均通过。

## 7. 本次未执行的操作

本次仅进行了目录、Git、配置、代码引用、运行进程和运行时数据的只读检查，并新增本计划文件；没有修改业务代码、删除/移动目录、创建数据库或迁移现有任务数据。
